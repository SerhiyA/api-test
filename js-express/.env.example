# Copy to .env for a host run (outside the dev container).
PORT=3001
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=12h

PGHOST=localhost
PGPORT=5432
PGUSER=bench
PGPASSWORD=bench
PGDATABASE=taskbench

REDIS_URL=redis://localhost:6379
CORS_ORIGIN=*
